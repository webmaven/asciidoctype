= AsciiDoctype Architecture
:toc: left
:toc-title: Contents
:toclevels: 4
:icons: font
:source-highlighter: highlight.js

This document describes the internal design, rendering pipeline, template
resolution strategy, and ASG node coverage of AsciiDoctype.

For development setup, coding standards, and day-to-day workflows, see
link:AGENTS.adoc[AGENTS.adoc]. For contribution process, see
link:CONTRIBUTING.adoc[CONTRIBUTING.adoc].

== 1. System Position in the Ecosystem

AsciiDoctype occupies the **formatting layer** between a semantic document
parser and an output orchestrator:

....
┌─────────────────────┐
│  Author (*.adoc)    │
└──────────┬──────────┘
           │  raw AsciiDoc text
           ▼
┌─────────────────────┐
│   AsciiDoctrine     │  Parser + TCK-compliant semantic graph
│  (ASG producer)     │
└──────────┬──────────┘
           │  Python dict (via to_dict())
           ▼
┌─────────────────────┐
│   AsciiDoctype      │  ← THIS LIBRARY
│ (Template renderer) │  Chameleon ZPT templates → HTML5 or XHTML strings
└──────────┬──────────┘
           │  markup string fragments / full document string
           ▼
┌──────────────────────────────┐
│  Golem SSG / EPUB Compiler / │  Orchestrator: writes files, manages assets
│  Custom downstream tool      │
└──────────────────────────────┘
....

**Key invariant:** AsciiDoctype never reads from disk (beyond template lookup),
never writes to disk, and never modifies the ASG nodes it receives. It is a
pure in-memory transformation — dict in, string out.

== 2. Core Components

=== 2.1 `AsciiDoctypeRenderer` and `asciidoctype.render`

`AsciiDoctypeRenderer` is the primary rendering orchestrator. It is constructed once
per rendering session (or once per target format) and reused across many `render()` calls.

For one-off renders, `asciidoctype.render(node, target_format="html5")` provides a
top-level functional shortcut.

[source,python]
----
import asciidoctype

# Top-level functional shortcut
html = asciidoctype.render(node, target_format="html5")

# Class orchestrator
renderer = asciidoctype.AsciiDoctypeRenderer(
    target_format="html5",         # or "xhtml"
    search_paths=["theme/"],       # optional, ordered str or Path objects
    highlighter=my_highlighter,    # optional Callable[[str, str], Optional[str]]
)
----

**Responsibilities:**

* Validate `target_format` and raise `ValueError` on bad values.
* Build the ordered search path list, normalizing `str` and `Path` entries and appending the bundled core fallback last.
* Initialise a `chameleon.PageTemplateLoader` over the resolved paths.
* Memoize candidate template resolution in `_template_cache` for sub-millisecond node dispatch.
* Dispatch each incoming ASG node dict to the correct template via `render()`.
* Wrap any template runtime error in `AsciiDoctypeRenderingError` for clean
  diagnostics.

=== 2.2 `AsciiDoctypeRenderingError`

A typed sentinel exception that carries the failing node name, the target
pipeline, and the root cause. Callers can catch this to implement graceful
degradation without catching every possible Python exception.

=== 2.3 Chameleon `PageTemplateLoader` & Dispatch Caching

Manages the compiled-bytecode template cache. Templates are compiled to Python
bytecode on first use; subsequent calls reuse the cache. In addition, `AsciiDoctypeRenderer`
memoizes resolved template references in `_template_cache` keyed on `(node_name, type, variant)`,
giving ultra-fast rendering throughput even on deeply nested documents.

=== 2.4 MathML Conversion (`latex2mathml`)

AsciiDoctype relies on `latex2mathml` as a runtime dependency. The `stem` and
`passthrough` (with `latexmath` style) templates use `latex2mathml` at render
time to convert LaTeX equations into native MathML markup, eliminating the need
for client-side JavaScript rendering engines like MathJax or KaTeX.

=== 2.5 Template Security Auditing (`asciidoctype.linter`)

Static analysis utilities (`audit_search_paths`, `audit_template`, and `TemplateFinding`)
audit custom template override directories for syntax errors and unsafe `structure` directives.

== 3. Template Resolution Pipeline

Template lookup follows a strict precedence order at every `render()` call:

....
Caller search_paths[0]   →  search_paths[1]  →  …  →  core_templates/<fmt>/
(user overrides)            (active theme)          (bundled fallback)
....

The algorithm:

1. Compute `node["name"]` (e.g. `"paragraph"`).
2. Call `self.loader["paragraph.html"]`.
3. Chameleon's `PageTemplateLoader` searches its path list in order and returns
   the first match.
4. If no match is found anywhere, `ValueError` is caught internally and
   `fallback_container.html` is used instead.
5. The resolved template is executed with `(node=node, renderer=self, ctx=ctx)`.

This means a theme can override any single template file without providing all
of them — missing overrides silently fall back to the bundled core.

=== 3.1 Template Variables Available in Every Template

[cols="1,1,3",options="header"]
|===
|Variable |Type |Description

|`node`
|`dict`
|The current ASG node dictionary being rendered. Shape varies by node type
(see <<asg-node-schemas>>).

|`renderer`
|`AsciiDoctypeRenderer`
|The renderer instance. Used to call `renderer.render(child, ctx)` for
recursive child rendering.

|`ctx`
|`dict`
|Arbitrary caller-supplied context (e.g. site settings, base URL). Empty dict
if not provided. Templates may read from it but must not mutate it.
|===

=== 3.2 Recursive Child Rendering in Templates

All child node lists are rendered by calling back into the renderer from inside
the template. The `structure` modifier is **mandatory** to prevent Chameleon
from HTML-escaping the returned markup string:

[source,xml]
----
<tal:block tal:repeat="inline node.get('inlines', [])"
           tal:replace="structure renderer.render(inline, ctx)" />
----

Without `structure`, child tags would appear as literal escaped text
(`&lt;strong&gt;`) instead of live markup.

== 4. Dual Rendering Pipelines

=== 4.1 HTML5 Pipeline

Target: modern browsers, progressive web apps.

* Void elements have no closing slash (`<br>`, `<img>`, `<meta>`).
* Document opens with `<!DOCTYPE html>`.
* No XML declaration or namespace binding.

=== 4.2 XHTML 1.0 Strict Pipeline

Target: EPUB 2/3 readers, Kindle, strict XML parsers.

* XML declaration: `<?xml version="1.0" encoding="UTF-8"?>`
* DOCTYPE: `<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN" …>`
* Root namespace: `<html xmlns="http://www.w3.org/1999/xhtml">`
* `<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />`
* All void elements explicitly self-closed.

== [[asg-node-schemas]]5. ASG Node Schemas and Template Coverage

All nodes are Python dicts produced by AsciiDoctrine's `to_dict()` method.
AsciiDoctype receives them as plain dictionaries — it has no knowledge of any
class hierarchy.

=== 5.1 Block-Level Nodes

==== `document`

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"document"`
|Fixed discriminator.

|`type`
|`"block"`
|Fixed.

|`header`
|`dict`
|Contains `"title"` (string). May be empty.

|`blocks`
|`List[dict]`
|Top-level block children (sections, paragraphs, …).

|`attributes`
|`dict`
|Document-level attribute entries.
|===

Template: `document.html` +
HTML5 output: Full `<!DOCTYPE html>`, `<html>`, `<head>`, `<title>`, `<body>`. +
XHTML output: As above plus XML declaration, DOCTYPE, and namespace.

**Coverage status:** ✅ Implemented

---

==== `section`

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"section"`
|Fixed.

|`type`
|`"block"`
|Fixed.

|`level`
|`int`
|Nesting depth starting at 1. Level 1 → `<h2>`, level 2 → `<h3>`, etc.

|`title`
|`List[dict]`
|Inline nodes forming the heading text.

|`blocks`
|`List[dict]`
|Child blocks within this section.

|`attributes`
|`dict`
|Optional `id`, `role`, etc.
|===

Template: `section.html` +
Output: `<section class="adoc-section level-{N}" id="…">` wrapping an
`<h{N+1}>` heading and a `<div class="section-blocks">` child container.

**Coverage status:** ✅ Implemented (levels 1–6)

---

==== `paragraph`

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"paragraph"`
|Fixed.

|`type`
|`"block"`
|Fixed.

|`inlines`
|`List[dict]`
|Inline content nodes.

|`attributes`
|`dict`
|Optional `id`, `role`.
|===

Template: `paragraph.html` +
Output: `<p id="…" class="…">…inline content…</p>`

**Coverage status:** ✅ Implemented

---

==== `listing`

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"listing"`
|Fixed.

|`type`
|`"block"`
|Fixed.

|`form`
|`"delimited"`
|Block form marker.

|`delimiter`
|`str`
|The opening delimiter string (e.g. `"----"`).

|`inlines`
|`List[dict]`
|Source code content inline text nodes and callouts.

|`title`
|`List[dict]`
|Optional caption inline nodes.

|`attributes`
|`dict`
|Optional `id`, `language`.
|===

Template: `listing.html` +
Output: `<div class="listingblock">` containing optional `<div class="title">`,
then `<pre class="highlight {lang}"><code class="language-{lang}">…</code></pre>`.

**Coverage status:** ✅ Implemented

---

==== `list`

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"list"`
|Fixed.

|`variant`
|`"unordered"` or `"ordered"`
|Drives `<ul>` vs. `<ol>`.

|`marker`
|`str`
|Original source marker (`*`, `.`, `-`, …).

|`items`
|`List[dict]`
|`listItem` node dicts.
|===

Template: `list.html` +
Output: `<ul>` or `<ol>` depending on `variant`, wrapping `<li>` items.

**Coverage status:** ✅ Implemented

---

==== `listItem`

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"listItem"`
|Fixed.

|`principal`
|`List[dict]`
|Primary inline content.

|`blocks`
|`List[dict]`
|Nested continuation blocks.
|===

Template: `listItem.html` +
Output: `<li>` containing principal inlines and optional continuation blocks.

**Coverage status:** ✅ Implemented

---

==== `admonition`

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"admonition"`
|Fixed.

|`variant`
|`"note"`, `"tip"`, `"important"`, `"warning"`, `"caution"`
|Admonition label.

|`blocks`
|`List[dict]`
|Body blocks.

|`title`
|`List[dict]`
|Optional title inlines.
|===

Template: `admonition.html` +
Output: `<div class="admonitionblock {variant}">` containing optional title and body blocks.

**Coverage status:** ✅ Implemented

---

==== `table`

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"table"`
|Fixed.

|`rows`
|`List[dict]`
|Row block dictionaries containing `cells` list of cell dictionaries (`colspan`, `rowspan`, `blocks`/`inlines`).

|`header_rows`
|`List[dict]`
|Optional explicit header row block dictionaries (or derived from `options="header"`).

|`attributes`
|`dict`
|Optional `id`, `cols`, `options`, `role`.
|===

Template: `table.html` +
Output: `<table>` containing optional `<caption>`, `<thead>`, `<tbody>`, `<tr>`, `<th>`, and `<td>` elements.

**Coverage status:** ✅ Implemented


---

==== `sidebar`

Template: `sidebar.html` +
Output: `<div class="sidebarblock">` containing optional title and body blocks.

**Coverage status:** ✅ Implemented

---

==== `example`

Template: `example.html` +
Output: `<div class="exampleblock">` containing optional title and body blocks.

**Coverage status:** ✅ Implemented

---

==== `quote`

Template: `quote.html` +
Output: `<div class="quoteblock">` containing `<blockquote>` and optional attribution.

**Coverage status:** ✅ Implemented

---

==== `image`

Template: `image.html` +
Output: `<div class="imageblock">` wrapping `<img>` (void element slash omitted in HTML5, self-closing in XHTML).

**Coverage status:** ✅ Implemented

---

==== `open`

Template: `open.html` +
Output: `<div class="openblock">` containing optional title and content blocks.

**Coverage status:** ✅ Implemented

---

==== `literal`

Template: `literal.html` +
Output: `<div class="literalblock">` containing `<pre class="literalblock">` with raw pre-formatted text.

**Coverage status:** ✅ Implemented

---

==== `verse`

Template: `verse.html` +
Output: `<div class="verseblock">` containing `<pre class="content">` and optional attribution.

**Coverage status:** ✅ Implemented

---

==== `stem`

Template: `stem.html` +
Output: `<div class="stemblock">` containing equation content. For `latexmath` variant, converts to native MathML using `latex2mathml`. For `asciimath` variant, emits raw text as a placeholder.

**Coverage status:** ✅ Implemented

---

==== `passthrough`

Template: `passthrough.html` +
Output: Emits node value as raw, unescaped markup. When `style` attribute is `latexmath`, converts to MathML like `stem`. Security note: raw HTML emission.

**Coverage status:** ✅ Implemented

---

==== `audio`

Template: `audio.html` +
Output: `<div class="audioblock">` wrapping `<audio src="..." controls="controls">`.

**Coverage status:** ✅ Implemented

---

==== `video`

Template: `video.html` +
Output: `<div class="videoblock">` wrapping `<video src="..." controls="controls">`.

**Coverage status:** ✅ Implemented

---

==== `collapsible`

Template: `collapsible.html` +
Output: `<details class="collapsible">` with `<summary>` and content.

**Coverage status:** ✅ Implemented

---

==== `descriptionList`

Template: `descriptionList.html` +
Output: `<dl class="dlist">` wrapping description list items.

**Coverage status:** ✅ Implemented

---

==== `descriptionListItem`

Template: `descriptionListItem.html` +
Output: Renders `<dt>` terms followed by `<dd>` definition content.

**Coverage status:** ✅ Implemented

---

==== `descriptionListTerm`

Template: `descriptionListTerm.html` +
Output: `<dt>` containing inline term content.

**Coverage status:** ✅ Implemented

---

==== `calloutList`

Template: `calloutList.html` +
Output: `<ol class="colist">` wrapping callout list items.

**Coverage status:** ✅ Implemented

---

==== `calloutListItem`

Template: `calloutListItem.html` +
Output: `<li>` containing callout item content.

**Coverage status:** ✅ Implemented

---

==== `floatingTitle`

Template: `floatingTitle.html` +
Output: Discrete heading (`<h1>`-`<h6>` with `class="discrete"`) not tied to a section.

**Coverage status:** ✅ Implemented

---

==== `thematic_break`

Template: `thematic_break.html` +
Output: `<hr>` (HTML5) / `<hr />` (XHTML).

**Coverage status:** ✅ Implemented

---

==== `page_break`

Template: `page_break.html` +
Output: `<div style="break-after: page;"></div>` for paged media.

**Coverage status:** ✅ Implemented

---

=== 5.2 Inline-Level Nodes

==== `text` (leaf node)

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"text"`
|Fixed.

|`type`
|`"string"`
|Fixed. Distinguishes leaf nodes from structural nodes.

|`value`
|`str`
|Raw text content. Emitted verbatim (no HTML escaping performed by the template
itself — escaping is handled upstream by AsciiDoctrine).
|===

Template: `text.html` +
Output: The bare string `node['value']`. No wrapping element.

**Coverage status:** ✅ Implemented

---

==== `span`

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"span"`
|Fixed.

|`type`
|`"inline"`
|Fixed.

|`variant`
|`"strong"`, `"emphasis"`, `"code"`, `"mark"`, or absent
|Drives element selection. Missing or unrecognised → `<span>`.

|`form`
|`"constrained"` or `"unconstrained"`
|Passed through; informational for downstream consumers.

|`inlines`
|`List[dict]`
|Child inline nodes.

|`attributes`
|`dict`
|Optional `role` (applied as CSS class).
|===

Template: `span.html` +
Output: `<strong>`, `<em>`, `<code>`, `<mark>`, or `<span>` depending on
`variant`, with optional `class` from `attributes.role`.

**Coverage status:** ✅ Implemented (strong, emphasis, code, mark, default)

---

==== `ref`

[cols="1,1,3",options="header"]
|===
|Key |Type |Notes

|`name`
|`"ref"`
|Fixed.

|`variant`
|`"link"`, `"xref"`, `"footnote"`
|Link type.

|`target`
|`str`
|URL or anchor id.

|`inlines`
|`List[dict]`
|Link text inline nodes.

|`attributes`
|`dict`
|Optional `role`, `window`, etc.
|===

Template: `ref.html` +
Output: `<a href="..." class="...">` inline link element.

**Coverage status:** ✅ Implemented

---

==== `image_inline`

Template: `image_inline.html` +
Output: `<span class="image-inline"><img ...></span>` inline image wrapper.

**Coverage status:** ✅ Implemented

---


==== `footnote`

Template: `footnote.html` +
Output: `<sup class="footnote" id="_footnote_id"><a href="#_footnotedef_num">num</a></sup>` footnote reference.

**Coverage status:** ✅ Implemented

---

==== `kbd`

Template: `kbd.html` +
Output: `<kbd>` containing key labels.

**Coverage status:** ✅ Implemented

---

==== `button`

Template: `button.html` +
Output: `<b class="button">` containing button label text.

**Coverage status:** ✅ Implemented

---

==== `menu`

Template: `menu.html` +
Output: `<span class="menuseq">` containing menu path with `<b class="menu">` and `<b class="menuitem">` elements.

**Coverage status:** ✅ Implemented

---

==== `callout`

Template: `callout.html` +
Output: `<i class="conum" data-value="N">` inline callout number marker.

**Coverage status:** ✅ Implemented

---

==== `break`

Template: `break.html` +
Output: `<br>` (HTML5) / `<br />` (XHTML) line break.

**Coverage status:** ✅ Implemented

---

=== 5.3 Fallback Container

Any node whose `name` has no corresponding `<name>.html` template is rendered
via `fallback_container.html`. It wraps the node's `blocks` or `inlines`
children in a `<div class="adoc-fallback node-<name>">` element, preserving
recursive rendering while clearly marking unsupported nodes in the output.

== 6. Directory Layout

....
asciidoctype/
├── bin/
│   ├── check.sh
│   └── generate-gallery.sh
├── examples/
│   └── gallery/
│       ├── generate_gallery.py
│       ├── gallery.css
│       └── index.html
├── src/
│   └── asciidoctype/
│       ├── __init__.py              # Public API exports
│       ├── exceptions.py            # Rendering, Security & Warning types
│       ├── linter.py                # Template static analysis & security auditor
│       ├── renderer.py              # AsciiDoctypeRenderer dispatch engine
│       └── core_templates/
│           ├── html5/               # HTML5 pipeline templates
│           │   ├── admonition.html
│           │   ├── audio.html
│           │   ├── break.html
│           │   ├── button.html
│           │   ├── callout.html
│           │   ├── calloutList.html
│           │   ├── calloutListItem.html
│           │   ├── collapsible.html
│           │   ├── descriptionList.html
│           │   ├── descriptionListItem.html
│           │   ├── descriptionListTerm.html
│           │   ├── document.html
│           │   ├── example.html
│           │   ├── fallback_container.html
│           │   ├── floatingTitle.html
│           │   ├── footnote.html
│           │   ├── image.html
│           │   ├── image_inline.html
│           │   ├── kbd.html
│           │   ├── list.html
│           │   ├── listItem.html
│           │   ├── listing.html
│           │   ├── literal.html
│           │   ├── menu.html
│           │   ├── open.html
│           │   ├── page_break.html
│           │   ├── paragraph.html
│           │   ├── passthrough.html
│           │   ├── quote.html
│           │   ├── ref.html
│           │   ├── section.html
│           │   ├── sidebar.html
│           │   ├── span.html
│           │   ├── stem.html
│           │   ├── table.html
│           │   ├── text.html
│           │   ├── thematic_break.html
│           │   ├── title.html
│           │   ├── verse.html
│           │   └── video.html
│           └── xhtml/               # XHTML 1.0 Strict pipeline templates

│               ├── admonition.html
│               ├── audio.html
│               ├── break.html
│               ├── button.html
│               ├── callout.html
│               ├── calloutList.html
│               ├── calloutListItem.html
│               ├── collapsible.html
│               ├── descriptionList.html
│               ├── descriptionListItem.html
│               ├── descriptionListTerm.html
│               ├── document.html
│               ├── example.html
│               ├── fallback_container.html
│               ├── floatingTitle.html
│               ├── footnote.html
│               ├── image.html
│               ├── image_inline.html
│               ├── kbd.html
│               ├── list.html
│               ├── listItem.html
│               ├── listing.html
│               ├── literal.html
│               ├── menu.html
│               ├── open.html
│               ├── page_break.html
│               ├── paragraph.html
│               ├── passthrough.html
│               ├── quote.html
│               ├── ref.html
│               ├── section.html
│               ├── sidebar.html
│               ├── span.html
│               ├── stem.html
│               ├── table.html
│               ├── text.html
│               ├── thematic_break.html
│               ├── title.html
│               ├── verse.html
│               └── video.html

├── tests/
│   ├── conftest.py
│   ├── test_admonition_sidebar.py
│   ├── test_complex_lists.py
│   ├── test_environment.py
│   ├── test_example_quote.py
│   ├── test_html5_blocks.py
│   ├── test_html5_document.py
│   ├── test_html5_inline.py
│   ├── test_image_nodes.py
│   ├── test_inline_macros.py
│   ├── test_list_nodes.py
│   ├── test_media_interactive.py
│   ├── test_ref_footnote.py
│   ├── test_renderer.py
│   ├── test_special_blocks.py
│   ├── test_table_node.py
│   ├── test_template_overrides.py
│   └── test_xhtml_rendering.py
├── .pre-commit-config.yaml
├── ARCHITECTURE.adoc
├── CONTRIBUTING.adoc
├── AGENTS.adoc
├── README.adoc
├── LICENSE.adoc
└── pyproject.toml
....

== 7. Design Constraints

* **No file I/O in production code.** AsciiDoctype only reads template files;
  it never writes anything to disk.
* **No state mutation.** The `ctx` dict and `node` dicts passed to `render()`
  are treated as read-only.
* **O(N) rendering complexity.** Cost scales linearly with the number of nodes
  in the document tree.
* **No styling.** AsciiDoctype emits clean semantic markup. CSS, fonts, and
  design tokens are downstream concerns.
* **Dual template rule.** Every supported node type requires **two** template
  files: one in `core_templates/html5/` and one in `core_templates/xhtml/`.
  A single-pipeline implementation is not acceptable.
* **Python-style attribute access in templates.** Chameleon template variables
  bound to plain Python dicts must use Python expression syntax
  (`node.get('attributes', {}).get('id')`), not TAL path syntax
  (`node/attributes/id`) which fails on plain dicts. TAL path syntax _does_
  work on Chameleon-provided objects that support attribute traversal — for
  example, `repeat/item/index` is valid because `repeat` is Chameleon's own
  `RepeatDict`, not a plain `dict`. The rule applies specifically to `node`,
  `ctx`, and any other plain-dict variable passed into the template namespace.
* **`structure` keyword for child rendering.** Child node output inside
  templates must use the `structure` modifier to prevent HTML-escaping of
  the returned markup string.
* **No trailing newlines in inline templates.** Chameleon preserves whitespace.
  Templates for inline nodes (`text.html`, `span.html`, `ref.html`, etc.) must
  not end with a newline or contain surrounding whitespace that would corrupt
  the rendered text stream.
* **Python 3.9+.** Uses `dict` type hints without `typing.Dict` for conciseness.
  Must remain compatible with CPython 3.9 through current.

== 8. Downstream Extension Points

Downstream systems (Golem SSG, EPUB compilers, etc.) can extend the rendering
pipeline without modifying AsciiDoctype:

=== 8.1 Custom Templates via `search_paths`

The most common pattern: provide a theme directory with custom `.html` template
files. The templates receive the same `node`, `renderer`, and `ctx` variables.

=== 8.2 Custom ASG Nodes

A Golem plugin may inject additional node types into the ASG (e.g. a Mermaid
diagram node named `"mermaid"`). As long as the theme provides `mermaid.html`,
AsciiDoctype will render it without modification. No core code change required.

=== 8.3 CSS Classes and IDs

Templates may emit arbitrary CSS classes and `id` attributes to allow
downstream styling without AsciiDoctype carrying any style responsibility.
For example, a theme's `section.html` might add `data-` attributes for
JavaScript-driven interactive features.

=== 8.4 Context Dictionary

The `ctx` dict flows through every recursive call. A Golem orchestrator might
populate it with site-level settings (`base_url`, `locale`, navigation data)
that templates can read to produce context-aware output.
